/**
 * Named constants for financial calculations.
 * Centralizes magic numbers to improve maintainability and consistency.
 */

/** Default inflation rate (%) when no micro plan provides it */
export const DEFAULT_INFLATION_PERCENT = 6;

/** Default expected return rate (%) when no micro plan provides it */
export const DEFAULT_EXPECTED_RETURN_PERCENT = 8;

/** Default maximum age (limit_age) for plan calculations when not specified */
export const DEFAULT_LIMIT_AGE = 110;
